// Barrel export for shared types. Re-export feature type modules here as they are added.
// See docs/PROJECT_STRUCTURE.md — src/types/ is the dependency-graph leaf (imports nothing from src).

export {};
