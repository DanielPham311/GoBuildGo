import { notImplemented } from "@/shared/api/response";

// GET /api/v1/admin/stats — dashboard metrics (API_DESIGN.md §10).
export async function GET() {
  return notImplemented("GET /api/v1/admin/stats");
}
