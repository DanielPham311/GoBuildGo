import { notImplemented } from "@/shared/api/response";

// PATCH /api/v1/admin/components/[id] — update component (API_DESIGN.md §10).
export async function PATCH() {
  return notImplemented("PATCH /api/v1/admin/components/[id]");
}

// DELETE /api/v1/admin/components/[id] — delete component.
export async function DELETE() {
  return notImplemented("DELETE /api/v1/admin/components/[id]");
}
