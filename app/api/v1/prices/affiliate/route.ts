import { notImplemented } from "@/shared/api/response";

// GET /api/v1/prices/affiliate — resolve a signed affiliate link + track click (API_DESIGN.md §7).
export async function GET() {
  return notImplemented("GET /api/v1/prices/affiliate");
}
