import { notImplemented } from "@/shared/api/response";

// GET /api/v1/prices/compare — compare prices across shops (API_DESIGN.md §7).
export async function GET() {
  return notImplemented("GET /api/v1/prices/compare");
}
