import { notImplemented } from "@/shared/api/response";

// POST /api/v1/setups/[id]/like — toggle like on a setup (API_DESIGN.md §5).
export async function POST() {
  return notImplemented("POST /api/v1/setups/[id]/like");
}
