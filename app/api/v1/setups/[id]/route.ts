import { notImplemented } from "@/shared/api/response";

// GET /api/v1/setups/[id] — setup detail (API_DESIGN.md §5).
export async function GET() {
  return notImplemented("GET /api/v1/setups/[id]");
}

// PATCH /api/v1/setups/[id] — update setup.
export async function PATCH() {
  return notImplemented("PATCH /api/v1/setups/[id]");
}

// DELETE /api/v1/setups/[id] — delete setup.
export async function DELETE() {
  return notImplemented("DELETE /api/v1/setups/[id]");
}
