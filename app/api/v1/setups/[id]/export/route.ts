import { notImplemented } from "@/shared/api/response";

// GET /api/v1/setups/[id]/export — export setup as image (API_DESIGN.md §5).
export async function GET() {
  return notImplemented("GET /api/v1/setups/[id]/export");
}
