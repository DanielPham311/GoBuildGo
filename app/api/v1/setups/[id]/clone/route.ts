import { notImplemented } from "@/shared/api/response";

// POST /api/v1/setups/[id]/clone — clone a setup (API_DESIGN.md §5).
export async function POST() {
  return notImplemented("POST /api/v1/setups/[id]/clone");
}
