import { notImplemented } from "@/shared/api/response";

// GET /api/v1/users/me/setups — current user's saved setups (API_DESIGN.md §8).
export async function GET() {
  return notImplemented("GET /api/v1/users/me/setups");
}
