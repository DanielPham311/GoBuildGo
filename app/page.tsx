export default function HomePage() {
  return (
    <main className="container flex min-h-screen flex-col items-center justify-center gap-4 text-center">
      <h1 className="text-4xl font-bold tracking-tight">gobuildgo</h1>
      <p className="max-w-md text-muted-foreground">
        Plan your dream desk setup — pick a room, choose a theme, add
        components, and shop everything in one place.
      </p>
    </main>
  );
}
